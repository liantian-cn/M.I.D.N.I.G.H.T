--[[
文件定位：
  颜色配置模块，承载 Cell/MegaCell/BadgeCell 的颜色语义映射表。

输入来源：
  由 color_palette 文档和协议草案中的颜色语义要求共同定义。

输出职责：
  对外提供 colors 配置域，供 cells 与渲染流程统一读取颜色定义。

生命周期/调用时机：
  在配置链路中早期加载，确保所有绘制相关模块共享同一来源。

约束与非目标：
  当前不实现颜色计算和容差策略，仅声明配置边界。

状态：
  draft
]]
