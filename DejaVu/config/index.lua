--[[
文件定位：
  配置聚合模块，用于汇总 core/layout/colors/debug 四类配置域。

输入来源：
  来自 config 子目录内各配置模块提供的配置表或占位数据结构。

输出职责：
  对外暴露统一配置入口，降低调用方对具体配置文件路径的耦合。

生命周期/调用时机：
  在配置模块末尾加载，供 events/matrix/cells/debug 等模块统一引用。

约束与非目标：
  当前仅保留聚合职责说明，不实现实际合并或默认值逻辑。

状态：
  draft
]]
