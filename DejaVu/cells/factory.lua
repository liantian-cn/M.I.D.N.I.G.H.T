--[[
文件定位：
  单元工厂模块，用于统一管理 Cell/MegaCell/BadgeCell 的选择与创建入口。

输入来源：
  依赖 cells 子目录中的三类单元模块，以及配置层单元规格参数。

输出职责：
  对外提供统一工厂接口，降低上层流程对具体单元实现的直接依赖。

生命周期/调用时机：
  在渲染流程初始化后由上层按槽位类型调用（后续实现）。

约束与非目标：
  当前不实现类型分发和实例缓存，仅定义工厂职责边界。

状态：
  draft
]]
