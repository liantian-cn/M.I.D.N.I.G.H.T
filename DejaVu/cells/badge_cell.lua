--[[
文件定位：
  BadgeCell 构建模块，负责带角标语义单元的构建职责定义。

输入来源：
  依赖 cell_spec 中 Badge 角标象限约定与颜色语义定义。

输出职责：
  对外提供 BadgeCell 构建边界，供上层业务表达附加状态类别。

生命周期/调用时机：
  在需要主语义 + 角标语义的槽位上按需调用（后续实现）。

约束与非目标：
  当前不实现角标绘制和冲突处理，仅保留模块职责注释。

状态：
  draft
]]
