interface:
  display_name: "Terminal Coder"
  short_description: "Handle MIDNIGHT Terminal Python, docs, and workflow tasks"
  default_prompt: "Use $terminal-coder to handle a MIDNIGHT Terminal task end to end."

policy:
  allow_implicit_invocation: false
